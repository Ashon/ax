# shopflow (forced-role variant)

Same domain-shaped fixture as `init_domain_auto` — the fixture
would naturally suggest a domain-centric team. This scenario
forces the setup agent to use a role-centric axis via `--axis role`
and asserts the override is respected despite the project hints
pointing the other way.
