# shop-reconfigure

Project started with a role-axis team (`frontend` + `backend`) and
has since grown to include a Terraform stack under `infra/`. The
reconfigure pass should notice the new directory, add a workspace
for it, and preserve the role axis.
