Dispatch two parallel tasks via start_task and do not write any files yourself. Task 1 goes to alpha: create alpha/result.txt containing exactly the single word `alpha`. Task 2 goes to beta: create beta/result.txt containing exactly the single word `beta`. Neither file should contain trailing whitespace or extra lines. Finish the run only after both tasks are marked complete and both files exist.
