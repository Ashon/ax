# web-shop

A small monorepo for a web application. The repository is
deliberately split by **implementation layer** rather than business
feature: the UI code lives in `frontend/`, the HTTP service in
`backend/`, the Terraform stack in `infra/`. Different engineers own
each layer.
