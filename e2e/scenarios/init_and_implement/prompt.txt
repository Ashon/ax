Implement greeter/hello.sh — a POSIX /bin/sh script that prints exactly the line `hello, ax` (with a trailing newline) when executed, and must be chmod +x. Delegate this via start_task to the workspace that owns the greeter/ directory (inspect your workspace list and pick the one whose dir is `greeter`). Do not write the file yourself. Finish the run only after the worker's task is marked completed and greeter/hello.sh passes a quick sanity check from its owner.
