# greeter

The only component in this repository. Contains POSIX `/bin/sh`
greeter scripts — all implementation files live here. There is no
documentation, infrastructure, or service workspace outside this
directory; this repo is intentionally a single subtree owned by one
team.
